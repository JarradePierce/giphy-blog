<div class='jumbotron' id='user-jumbo'>
  <h2 class='username-header'><%= @user.username %></h2>
  <h4 class='user-points'>Points: <%= @user.points %></h4>
</div>

<div class="menu-container">
<% if current_user.id == @user.id %>
  <ul class="nav nav-pills menu-ul justify-content-center">
    <li class='nav-link profile-menu-link'><%= button_to "New Note", new_user_blog_path(@user), method: "get", class: "btn new-blog-button profile-menu-links-button" %></li>
    <li class='nav-link profile-menu-link'><%= button_to "Categories", user_categories_path(@user), method: "get", class: "btn categories-button profile-menu-links-button" %></li>
    <li class='nav-link profile-menu-link'>
      <%= button_to "Completed", completed_path(@user), method: "get", class: "btn completed-button profile-menu-links-button" %>
    </li>
    <li class='nav-link profile-menu-link'>
      <%= button_to "Incomplete", incomplete_path(@user), method: "get", class: "btn incomplete-button profile-menu-links-button" %>
    </li>
  </ul>
<% end %>
</div>
